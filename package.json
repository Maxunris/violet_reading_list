{
  "name": "violet-reading-list",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "test:extension:verify": "node tests/manual/extension-verify.mjs",
    "test:all": "npm test && npm run build && npm run test:extension:verify"
  },
  "devDependencies": {
    "playwright": "^1.58.2"
  }
}
